# Landing page screenshots

Drop these four iPhone screenshots here. The login/landing page will
pick them up automatically and render each inside a CSS iPhone frame.

| Filename | What to put here |
|---|---|
| `screen-dashboard.jpg`  | The "Good afternoon, Nicholas" Dashboard with the Tokyo image + 16 days countdown |
| `screen-trips.jpg`      | The Trip detail — "June 2026 Asia Business Trip" with the itinerary list |
| `screen-lounges.jpg`    | The Lounges page with the stat cards (Programs Linked / Airports Indexed) |
| `screen-forwarding.jpg` | The Forwarding Desk — "Forward a booking. Snap a receipt." |

Source resolution: 945×2048 (iPhone screenshot). Other sizes work too;
the frame uses `aspect-ratio: 945 / 2048` and `object-fit: cover`.

JPG is preferred (smaller than PNG for photo-like content). Aim for
~150–300 KB each so the landing page loads fast.
